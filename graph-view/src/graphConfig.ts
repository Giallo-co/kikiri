import type { GraphConfig } from './types'

export const graphConfig: GraphConfig = {
  nodeSize: 6,
  linkThickness: 1,
  centerForce: 0.05,
  repelForce: -300,
  repelForcePercentage: 1.0,
  linkForce: 0.4,
  linkDistance: 60,
  showLabels: false,
}
